# deps
node_modules
.pnpm-store

# build artifacts
.next
.next-*
out
dist
.turbo
*.tsbuildinfo

# runtime
storage/
*.log

# env
.env
.env.local
.env.*.local
!.env.example

# editor / OS
.DS_Store
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# claude local state
.claude

# outreach runtime state (send-ledger, warmup) — never commit, contains PII
.state/

# next generated
next-env.d.ts

# docker volumes (if anyone maps locally)
.docker-data/

# testing
coverage/
test-results/
playwright-report/
playwright/.cache/

# pdf generation cache
.pdf-cache/

# scratch / scrape dumps / preview artifacts (not source)
.tmp_*
DesktopEINSWebsite.tmp_*
landtmp.html
Landingpage Icon.html
*.aep
apps/portal/review-preview*.html
uploads/pasted-*.png

# Vercel pulled production env (contains live secrets, never commit)
apps/portal/.vercel.prod.env
.vercel.prod.env
.vercel

# pentest playbook + change-ledger (untracked deliverables, never commit)
PENTEST-PLAN-*.md
.pentest-ledger-*.md
